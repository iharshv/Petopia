{
  "name": "petopia",
  "version": "1.0.0",
  "description": "Petopia - Pet Shop and Services",
  "main": "index.js",
  "scripts": {
    "start": "live-server . --port=3000 --entry-file=index.html",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "live-server": "^1.2.0"
  }
}